# 📋 Чек-лист для проверки работы студента

## 👋 Для проверяющего

Этот чек-лист поможет вам быстро и объективно оценить работу студента.

**Максимальный балл:** 30 (20 основных + 10 бонусных за тесты)

---

## ⚡ Быстрая проверка (5 минут)

### Шаг 1: Клонируйте репозиторий студента

```bash
git clone <URL_РЕПОЗИТОРИЯ_СТУДЕНТА>
cd <папка_проекта>
```

### Шаг 2: Соберите проект

```bash
mvn clean package
```

**✅ Ожидаемый результат:** `BUILD SUCCESS`

**❌ Если ошибка:**
- Проверьте `pom.xml` на наличие всех зависимостей
- Проверьте версию Java (должна быть 17)
- -2 балла за ошибки компиляции

### Шаг 3: Запустите тесты

```bash
mvn test
```

**✅ Ожидаемый результат:** Все тесты "зелёные"

**❌ Если есть "красные" тесты:**
- -1 балл за каждый проваленный тест
- Максимум -5 баллов

### Шаг 4: Разверните на Tomcat

```bash
# Остановите Tomcat
catalina stop

# Удалите старое приложение
rm -rf $CATALINA_HOME/webapps/ROOT*

# Разверните
cp target/*.war $CATALINA_HOME/webapps/ROOT.war

# Запустите
catalina run
```

**✅ Ожидаемый результат:** Приложение запустилось без ошибок

### Шаг 5: Проверьте базовую работоспособность

```bash
curl http://localhost:8080/api/posts?search=&pageNumber=1&pageSize=10
```

**✅ Ожидаемый результат:** JSON с постами

---

## 📝 Детальная проверка функциональности

### Задание 1: Удаление лайка (5 баллов)

#### Проверка через curl

```bash
# Создать пост
POST_ID=$(curl -s -X POST http://localhost:8080/api/posts \
  -H "Content-Type: application/json" \
  -d '{"title":"Тест лайков","text":"Текст","tags":[]}' \
  | python3 -c "import sys, json; print(json.load(sys.stdin)['id'])")

# Добавить лайк
LIKES=$(curl -s -X POST http://localhost:8080/api/posts/$POST_ID/likes)
echo "После добавления: $LIKES"  # Должно быть 1

# Убрать лайк
LIKES=$(curl -s -X DELETE http://localhost:8080/api/posts/$POST_ID/likes)
echo "После удаления: $LIKES"  # Должно быть 0

# Попробовать убрать ещё раз (не должно стать отрицательным)
LIKES=$(curl -s -X DELETE http://localhost:8080/api/posts/$POST_ID/likes)
echo "После повторного удаления: $LIKES"  # Должно остаться 0
```

#### Критерии оценки

| Критерий | Баллы | Проверка |
|----------|-------|----------|
| Endpoint `DELETE /api/posts/{id}/likes` существует | 1 | curl не даёт 404 |
| Лайк корректно убирается | 2 | Счётчик уменьшается на 1 |
| Счётчик не становится отрицательным | 1 | После многократного DELETE остаётся 0 |
| Код написан правильно (см. ниже) | 1 | Проверка кода |

#### Что смотреть в коде

```java
// PostDao.java - метод добавлен
void decrementLikes(Long id);

// PostDaoImpl.java - правильная реализация
@Override
public void decrementLikes(Long id) {
    String sql = "UPDATE posts SET likes_count = GREATEST(likes_count - 1, 0) WHERE id = ?";
    jdbcTemplate.update(sql, id);
}

// PostService.java - метод добавлен
int decrementLikes(Long id);

// PostServiceImpl.java - правильная реализация с @Transactional
@Override
@Transactional
public int decrementLikes(Long id) {
    postDao.decrementLikes(id);
    Optional<Post> post = postDao.findById(id);
    return post.map(Post::getLikesCount).orElse(0);
}

// PostController.java - endpoint добавлен
@DeleteMapping("/{id}/likes")
public ResponseEntity<Integer> removeLike(@PathVariable Long id) {
    int likesCount = postService.decrementLikes(id);
    return ResponseEntity.ok(likesCount);
}
```

**Частые ошибки:**
- ❌ Нет `GREATEST(..., 0)` — счётчик может стать отрицательным
- ❌ Нет `@Transactional` в сервисе
- ❌ Endpoint возвращает не тот HTTP код

---

### Задание 2: Редактирование комментария (5 баллов)

#### Проверка через curl

```bash
# Создать комментарий
COMMENT_JSON=$(curl -s -X POST http://localhost:8080/api/posts/1/comments \
  -H "Content-Type: application/json" \
  -d '{"text":"Исходный текст","postId":1}')
COMMENT_ID=$(echo $COMMENT_JSON | python3 -c "import sys, json; print(json.load(sys.stdin)['id'])")

echo "Создан комментарий ID=$COMMENT_ID"

# Отредактировать комментарий
curl -s -X PUT http://localhost:8080/api/posts/1/comments/$COMMENT_ID \
  -H "Content-Type: application/json" \
  -d "{\"id\":$COMMENT_ID,\"text\":\"Отредактированный текст\",\"postId\":1}" \
  | python3 -m json.tool

# Проверить, что изменения сохранились
curl -s http://localhost:8080/api/posts/1/comments/$COMMENT_ID | python3 -m json.tool
```

#### Критерии оценки

| Критерий | Баллы | Проверка |
|----------|-------|----------|
| Endpoint `PUT /api/posts/{postId}/comments/{commentId}` существует | 1 | curl не даёт 404 |
| Комментарий корректно обновляется | 2 | Текст изменился в БД |
| Возвращается обновлённый комментарий | 1 | Response содержит новый текст |
| Код написан правильно | 1 | Проверка кода |

#### Что смотреть в коде

```java
// CommentDao.java
Comment update(Comment comment);

// CommentDaoImpl.java
@Override
public Comment update(Comment comment) {
    String sql = "UPDATE comments SET text = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?";
    jdbcTemplate.update(sql, comment.getText(), comment.getId());
    return findById(comment.getId()).orElseThrow();
}

// CommentService.java
Comment updateComment(Long postId, Long commentId, UpdateCommentRequest request);

// CommentServiceImpl.java
@Override
@Transactional
public Comment updateComment(Long postId, Long commentId, UpdateCommentRequest request) {
    Comment comment = commentDao.findById(commentId)
        .orElseThrow(() -> new RuntimeException("Comment not found"));
    comment.setText(request.getText());
    return commentDao.update(comment);
}

// CommentController.java
@PutMapping("/{commentId}")
public ResponseEntity<Comment> updateComment(
    @PathVariable Long postId,
    @PathVariable Long commentId,
    @RequestBody UpdateCommentRequest request
) {
    Comment comment = commentService.updateComment(postId, commentId, request);
    return ResponseEntity.ok(comment);
}
```

**Частые ошибки:**
- ❌ Не обновляется `updated_at`
- ❌ Нет проверки существования комментария
- ❌ Нет `@Transactional`

---

### Задание 3: Удаление комментария (5 баллов)

#### Проверка через curl

```bash
# Создать пост
POST_ID=$(curl -s -X POST http://localhost:8080/api/posts \
  -H "Content-Type: application/json" \
  -d '{"title":"Тест удаления","text":"Текст","tags":[]}' \
  | python3 -c "import sys, json; print(json.load(sys.stdin)['id'])")

# Создать комментарий
COMMENT_ID=$(curl -s -X POST http://localhost:8080/api/posts/$POST_ID/comments \
  -H "Content-Type: application/json" \
  -d "{\"text\":\"Удалю\",\"postId\":$POST_ID}" \
  | python3 -c "import sys, json; print(json.load(sys.stdin)['id'])")

echo "Создан комментарий ID=$COMMENT_ID к посту ID=$POST_ID"

# Проверить счётчик комментариев
COMMENTS_COUNT=$(curl -s http://localhost:8080/api/posts/$POST_ID \
  | python3 -c "import sys, json; print(json.load(sys.stdin)['commentsCount'])")
echo "Счётчик комментариев: $COMMENTS_COUNT"  # Должно быть 1

# Удалить комментарий
curl -s -X DELETE http://localhost:8080/api/posts/$POST_ID/comments/$COMMENT_ID

# Проверить, что комментария нет
COMMENTS=$(curl -s http://localhost:8080/api/posts/$POST_ID/comments)
echo "Комментарии: $COMMENTS"  # Должно быть []

# Проверить, что счётчик обновился
COMMENTS_COUNT=$(curl -s http://localhost:8080/api/posts/$POST_ID \
  | python3 -c "import sys, json; print(json.load(sys.stdin)['commentsCount'])")
echo "Счётчик после удаления: $COMMENTS_COUNT"  # Должно быть 0
```

#### Критерии оценки

| Критерий | Баллы | Проверка |
|----------|-------|----------|
| Endpoint `DELETE /api/posts/{postId}/comments/{commentId}` существует | 1 | curl не даёт 404 |
| Комментарий удаляется из БД | 2 | GET возвращает пустой массив |
| Счётчик комментариев обновляется | 1 | `commentsCount` уменьшается |
| Код написан правильно | 1 | Проверка кода |

#### Что смотреть в коде

```java
// CommentService.java
void deleteComment(Long postId, Long commentId);

// CommentServiceImpl.java
@Override
@Transactional
public void deleteComment(Long postId, Long commentId) {
    commentDao.delete(commentId);
    // Важно: должен обновляться счётчик в posts!
    postDao.decrementCommentsCount(postId);
}

// CommentController.java
@DeleteMapping("/{commentId}")
public ResponseEntity<Void> deleteComment(
    @PathVariable Long postId,
    @PathVariable Long commentId
) {
    commentService.deleteComment(postId, commentId);
    return ResponseEntity.ok().build();
}
```

**Частые ошибки:**
- ❌ Не обновляется счётчик `comments_count` в `posts`
- ❌ Нет `@Transactional`
- ❌ Неправильный HTTP код ответа

---

### Задание 4: Каскадное удаление поста (5 баллов)

#### Проверка через curl

```bash
# Создать пост
POST_ID=$(curl -s -X POST http://localhost:8080/api/posts \
  -H "Content-Type: application/json" \
  -d '{"title":"Тест каскадного удаления","text":"Текст","tags":[]}' \
  | python3 -c "import sys, json; print(json.load(sys.stdin)['id'])")

# Создать 3 комментария
for i in {1..3}; do
  curl -s -X POST http://localhost:8080/api/posts/$POST_ID/comments \
    -H "Content-Type: application/json" \
    -d "{\"text\":\"Комментарий $i\",\"postId\":$POST_ID}" > /dev/null
done

# Проверить, что комментарии созданы
COMMENTS_COUNT=$(curl -s http://localhost:8080/api/posts/$POST_ID/comments | python3 -c "import sys, json; print(len(json.load(sys.stdin)))")
echo "Создано комментариев: $COMMENTS_COUNT"  # Должно быть 3

# Удалить пост
curl -s -X DELETE http://localhost:8080/api/posts/$POST_ID

# Проверить, что пост удалён
HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/posts/$POST_ID)
echo "HTTP код при попытке GET поста: $HTTP_CODE"  # Должно быть 404

# Проверить, что комментарии тоже удалены
COMMENTS=$(curl -s http://localhost:8080/api/posts/$POST_ID/comments)
echo "Комментарии после удаления поста: $COMMENTS"  # Должно быть []
```

#### Критерии оценки

| Критерий | Баллы | Проверка |
|----------|-------|----------|
| При удалении поста удаляются комментарии | 3 | GET comments возвращает [] |
| Нет "осиротевших" комментариев в БД | 1 | Проверка через H2 консоль |
| Код написан правильно с транзакцией | 1 | Проверка кода |

#### Что смотреть в коде

```java
// PostServiceImpl.java
@Override
@Transactional  // ОБЯЗАТЕЛЬНО!
public void deletePost(Long id) {
    // Сначала удаляем комментарии
    commentService.deleteAllCommentsByPostId(id);
    // Потом пост
    postDao.delete(id);
}

// CommentService.java
void deleteAllCommentsByPostId(Long postId);

// CommentServiceImpl.java
@Override
@Transactional
public void deleteAllCommentsByPostId(Long postId) {
    commentDao.deleteAllByPostId(postId);
}

// CommentDao.java
void deleteAllByPostId(Long postId);

// CommentDaoImpl.java
@Override
public void deleteAllByPostId(Long postId) {
    String sql = "DELETE FROM comments WHERE post_id = ?";
    jdbcTemplate.update(sql, postId);
}
```

**Частые ошибки:**
- ❌ Нет `@Transactional` — если упадёт, будет inconsistent state
- ❌ Комментарии удаляются ПОСЛЕ поста (должны ДО из-за foreign key)
- ❌ Метод `deleteAllByPostId` не реализован

---

## 🎓 Задание 5 (БОНУС): Unit-тесты (+10 баллов)

### Автоматическая проверка

```bash
# Запустить тесты с покрытием
mvn clean test jacoco:report

# Открыть отчёт
open target/site/jacoco/index.html
```

### Критерии оценки

| Критерий | Баллы | Проверка |
|----------|-------|----------|
| Тесты для `PostService.decrementLikes()` | 2 | Проверка файла |
| Тесты для `CommentService.updateComment()` | 2 | Проверка файла |
| Тесты для `CommentService.deleteComment()` | 2 | Проверка файла |
| Тесты для `CommentService.deleteAllCommentsByPostId()` | 2 | Проверка файла |
| Все тесты "зелёные" | 1 | `mvn test` |
| Использован Mockito правильно | 1 | Проверка кода |

### Что смотреть в тестах

#### Хороший тест (пример):

```java
@ExtendWith(MockitoExtension.class)
class PostServiceImplTest {
    @Mock
    private PostDao postDao;

    @InjectMocks
    private PostServiceImpl postService;

    @Test
    void decrementLikes_ShouldReturnUpdatedCount() {
        // Given
        Long postId = 1L;
        Post post = new Post();
        post.setId(postId);
        post.setLikesCount(5);

        when(postDao.findById(postId)).thenReturn(Optional.of(post));

        // When
        int result = postService.decrementLikes(postId);

        // Then
        assertEquals(5, result);
        verify(postDao).decrementLikes(postId);
        verify(postDao).findById(postId);
    }
}
```

**Проверяем:**
- ✅ Использует `@Mock` и `@InjectMocks`
- ✅ Есть `when()` для настройки mock'а
- ✅ Есть `verify()` для проверки вызовов
- ✅ Есть `assertEquals()` для проверки результата
- ✅ Структура Given-When-Then

**Частые ошибки в тестах:**
- ❌ Не использует Mockito (реальные объекты вместо mock'ов)
- ❌ Нет `verify()` — не проверяет вызовы методов
- ❌ Нет `assertions` — не проверяет результат
- ❌ Тесты "красные" (не проходят)

---

## 📊 Итоговая оценка

### Таблица баллов

| Раздел | Макс. баллы | Баллы студента |
|--------|-------------|----------------|
| Удаление лайка | 5 | ___ |
| Редактирование комментария | 5 | ___ |
| Удаление комментария | 5 | ___ |
| Каскадное удаление | 5 | ___ |
| Unit-тесты (бонус) | 10 | ___ |
| **ИТОГО** | **30** | **___** |

### Штрафы

| Проблема | Штраф |
|----------|-------|
| Проект не компилируется | -5 |
| Есть "красные" тесты | -1 за каждый (макс -5) |
| Нет логирования в новых методах | -1 |
| Нет @Transactional где нужно | -2 |
| SQL инъекция (не использует `?`) | -3 |
| Нет обработки ошибок | -1 |

### Шкала оценок

| Баллы | Оценка |
|-------|--------|
| 25-30 | Отлично (5) |
| 20-24 | Хорошо (4) |
| 15-19 | Удовлетворительно (3) |
| < 15 | На доработку (2) |

---

## 💡 Рекомендации для проверяющего

### 1. Начните с автоматической проверки

```bash
# Клонировать, собрать, запустить тесты
git clone <repo>
cd <repo>
mvn clean package
mvn test
```

Если всё "зелёное" — уже хороший знак!

### 2. Проверьте функциональность

Используйте готовые curl команды из этого чек-листа. Скопируйте и вставьте в терминал.

### 3. Просмотрите код

Обратите внимание на:
- Наличие `@Transactional`
- Параметризованные SQL (`?` вместо конкатенации)
- Обработку ошибок
- Логирование
- Комментарии (но не слишком много)

### 4. Дайте развёрнутый фидбек

Не просто "не работает", а:
- Что именно не работает
- Какой ожидался результат
- Что получилось на самом деле
- Подсказка, как исправить

**Пример хорошего фидбека:**

> ❌ Задание 1: Удаление лайка — 3/5 баллов
> 
> Endpoint работает, но счётчик может стать отрицательным.
> 
> В `PostDaoImpl.java` строка 45:
> ```java
> UPDATE posts SET likes_count = likes_count - 1 WHERE id = ?
> ```
> 
> Измените на:
> ```java
> UPDATE posts SET likes_count = GREATEST(likes_count - 1, 0) WHERE id = ?
> ```
> 
> Тогда при likes_count = 0 он не станет -1.

---

## 🔍 Проверка качества кода

### Code Review чек-лист

#### Структура кода
- [ ] Код логично разделён по слоям (Controller → Service → DAO)
- [ ] Нет дублирования кода
- [ ] Методы короткие и выполняют одну задачу

#### Именование
- [ ] Названия классов/методов/переменных понятные
- [ ] Используются английские имена
- [ ] Следуют Java Code Conventions

#### Обработка ошибок
- [ ] Используются исключения, а не null
- [ ] Есть try-catch где нужно
- [ ] Ошибки логируются

#### Транзакции
- [ ] `@Transactional` на методах сервиса, изменяющих данные
- [ ] Нет транзакций в контроллерах или DAO

#### SQL
- [ ] Используются параметризованные запросы (`?`)
- [ ] Нет SQL инъекций
- [ ] Оптимальные запросы (нет N+1 problem)

#### Тесты
- [ ] Покрывают основные сценарии
- [ ] Независимы друг от друга
- [ ] Используют mock'и правильно

---

## 📞 Часто задаваемые вопросы

### Студент использовал другой подход. Это ошибка?

Нет, если:
- ✅ Функциональность работает
- ✅ Код понятный и поддерживаемый
- ✅ Нет явных антипаттернов

Да, если:
- ❌ SQL инъекция
- ❌ Нет транзакций
- ❌ Хардкод значений

### Студент не написал тесты. Сколько баллов снять?

Тесты — это **бонус**. Если их нет, просто не добавляйте +10 баллов. Не снимайте баллы с основного задания.

### Студент реализовал дополнительную функциональность. Добавить баллы?

Можно добавить +1-3 бонусных балла за:
- Пагинацию комментариев
- Валидацию данных
- Красивые сообщения об ошибках
- Дополнительные эндпоинты

Но это не обязательно.

---

## ✅ Итоговый чек-лист проверки

### Перед отправкой фидбека студенту

- [ ] Проверил все 4 основных задания
- [ ] Проверил тесты (если есть)
- [ ] Заполнил таблицу баллов
- [ ] Написал развёрнутый фидбек с примерами
- [ ] Указал, что нужно исправить
- [ ] Дал советы по улучшению кода

### Критичные ошибки (обязательно указать)

- [ ] SQL инъекция
- [ ] Отсутствие транзакций
- [ ] "Красные" тесты
- [ ] Проект не компилируется

---

## 📧 Шаблон фидбека

```markdown
# Проверка проектной работы

## Общая оценка: ___/30 баллов

### ✅ Что сделано хорошо:
- ...
- ...

### ⚠️ Что нужно исправить:

#### Задание 1 (___/5):
- Проблема: ...
- Где: файл X, строка Y
- Как исправить: ...

#### Задание 2 (___/5):
- ...

### 💡 Рекомендации:
- ...

### Итог:
[ ] Принято
[ ] На доработку

Ревьюер: ___
Дата: ___
```

---

**Удачи в проверке! Если возникнут вопросы — обращайтесь к старшему преподавателю.**



