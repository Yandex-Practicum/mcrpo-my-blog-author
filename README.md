# 📘 My Blog - Авторское решение

## 🎯 Назначение

Это **полное авторское решение** проектной работы для проверяющих и преподавателей.

---

## 📋 Для проверяющих

### Быстрая проверка работы студента

1. **Откройте чек-лист:** [`ЧЕК-ЛИСТ_ДЛЯ_ПРОВЕРКИ.md`](./ЧЕК-ЛИСТ_ДЛЯ_ПРОВЕРКИ.md)
2. Следуйте инструкциям — там есть все curl команды для проверки
3. Сравните код студента с этим эталонным решением

### Что есть в этой папке

- ✅ Полностью реализованный backend
- ✅ Рабочий frontend (React)
- ✅ Все TODO выполнены
- ✅ Тесты написаны
- ✅ Документация
- ✅ Чек-лист для проверки

---

## 🚀 Запуск эталонного решения

### Вариант 1: Jetty (встроенный, рекомендуется)

```bash
# Backend
mvn jetty:run -Dmaven.test.skip=true

# В другом терминале - Frontend
# ⚠️ Требуется Node.js 18+: brew install node (macOS) или https://nodejs.org
cd frontend
npm install
npm run dev
```

**Откройте:** http://localhost:3000

### Вариант 2: Tomcat

```bash
# 1. Соберите WAR
mvn clean package -Dmaven.test.skip=true

# 2. Запустите на Tomcat (замените путь на свой):
cp target/my-blog-back-app.war /path/to/tomcat/webapps/ROOT.war
/path/to/tomcat/bin/startup.sh

# 3. Frontend - см. Вариант 1
```

### Вариант 3: Docker для frontend

```bash
# Frontend
cd frontend
docker compose up -d
```

**Откройте:** http://localhost:3000

Backend должен быть запущен на порту 8080.

---

## ✅ Реализованная функциональность

### Backend API

#### Посты
- ✅ GET `/api/posts` — список постов с поиском и пагинацией
- ✅ GET `/api/posts/{id}` — получение поста
- ✅ POST `/api/posts` — создание поста
- ✅ PUT `/api/posts/{id}` — редактирование поста
- ✅ DELETE `/api/posts/{id}` — удаление поста (с каскадным удалением комментариев)
- ✅ POST `/api/posts/{id}/likes` — добавить лайк
- ✅ **DELETE `/api/posts/{id}/likes`** — убрать лайк ⭐
- ✅ GET `/api/posts/{id}/image` — получить изображение
- ✅ PUT `/api/posts/{id}/image` — загрузить изображение

#### Комментарии
- ✅ GET `/api/posts/{postId}/comments` — список комментариев
- ✅ GET `/api/posts/{postId}/comments/{id}` — получение комментария
- ✅ POST `/api/posts/{postId}/comments` — создание комментария
- ✅ **PUT `/api/posts/{postId}/comments/{id}`** — редактирование комментария ⭐
- ✅ **DELETE `/api/posts/{postId}/comments/{id}`** — удаление комментария ⭐

⭐ — функциональность, которую студенты должны реализовать

### Frontend

- ✅ Просмотр ленты постов
- ✅ Создание/редактирование/удаление постов
- ✅ Лайки (toggle: добавление/удаление)
- ✅ Создание/редактирование/удаление комментариев
- ✅ Загрузка изображений
- ✅ Поиск и пагинация
- ✅ Адаптивный дизайн

---

## 🧪 Тестирование

### Запуск всех тестов

```bash
mvn test
```

### Тесты с покрытием

```bash
mvn clean test jacoco:report
open target/site/jacoco/index.html
```

### Что покрыто тестами

- ✅ PostService (unit-тесты)
- ✅ CommentService (unit-тесты)
- ✅ PostController (интеграционные тесты)
- ✅ CommentController (интеграционные тесты)

---

## 📁 Структура проекта

```
my-blog-author/
├── src/
│   ├── main/
│   │   ├── java/com/myblog/
│   │   │   ├── config/          # Spring конфигурация
│   │   │   ├── controller/      # REST контроллеры
│   │   │   ├── service/         # Бизнес-логика
│   │   │   ├── dao/             # Работа с БД
│   │   │   ├── model/           # Модели данных
│   │   │   ├── dto/             # Data Transfer Objects
│   │   │   ├── filter/          # CORS фильтр
│   │   │   └── exception/       # Обработка ошибок
│   │   ├── resources/
│   │   │   ├── schema.sql       # SQL схема
│   │   │   ├── application.properties
│   │   │   └── logback.xml
│   │   └── webapp/WEB-INF/
│   │       └── web.xml
│   └── test/                    # Тесты
├── frontend/                    # Frontend (React + Vite)
├── pom.xml                      # Maven
├── README.md                    # Этот файл
└── ЧЕК-ЛИСТ_ДЛЯ_ПРОВЕРКИ.md     # Для проверяющих
```

---

## 🔍 Ключевые реализации

### 1. Удаление лайка

**Файл:** `PostServiceImpl.java`

```java
@Override
@Transactional
public int decrementLikes(Long id) {
    log.debug("Decrementing likes for post with id: {}", id);
    postDao.decrementLikes(id);
    Optional<Post> post = postDao.findById(id);
    return post.map(Post::getLikesCount).orElse(0);
}
```

**SQL:** `PostDaoImpl.java`

```java
@Override
public void decrementLikes(Long id) {
    String sql = "UPDATE posts SET likes_count = GREATEST(likes_count - 1, 0) WHERE id = ?";
    jdbcTemplate.update(sql, id);
}
```

**Важно:** `GREATEST(..., 0)` не даёт счётчику стать отрицательным.

### 2. Редактирование комментария

**Файл:** `CommentServiceImpl.java`

```java
@Override
@Transactional
public Comment updateComment(Long postId, Long commentId, UpdateCommentRequest request) {
    log.debug("Updating comment {} for post {}", commentId, postId);
    Comment comment = commentDao.findById(commentId)
        .orElseThrow(() -> new RuntimeException("Comment not found: " + commentId));
    comment.setText(request.getText());
    return commentDao.update(comment);
}
```

### 3. Каскадное удаление поста

**Файл:** `PostServiceImpl.java`

```java
@Override
@Transactional
public void deletePost(Long id) {
    log.debug("Deleting post with id: {}", id);
    // Сначала удаляем комментарии
    commentService.deleteAllCommentsByPostId(id);
    // Затем пост
    postDao.delete(id);
}
```

**Важно:** Удаление комментариев **до** удаления поста (foreign key constraint).

---

## 🧩 Архитектура

```
┌─────────────────┐
│  Controller     │ ← REST endpoints
│  (@RestController)
└────────┬────────┘
         │
┌────────▼────────┐
│   Service       │ ← Бизнес-логика
│ (@Transactional)│
└────────┬────────┘
         │
┌────────▼────────┐
│     DAO         │ ← JdbcTemplate
│  (Repository)   │
└────────┬────────┘
         │
┌────────▼────────┐
│   Database      │ ← H2 in-memory
│      (H2)       │
└─────────────────┘
```

**Принципы:**
- ✅ Разделение ответственности (Separation of Concerns)
- ✅ Dependency Injection
- ✅ Транзакции на уровне сервиса
- ✅ Обработка ошибок через Global Exception Handler
- ✅ CORS настроен

---

## 📊 Сравнение с прекодом

| Функция | my-blog-pre | my-blog-author |
|---------|-------------|----------------|
| Удаление лайка | ❌ TODO | ✅ Реализовано |
| Редактирование комментария | ❌ TODO | ✅ Реализовано |
| Удаление комментария | ❌ TODO | ✅ Реализовано |
| Каскадное удаление | ❌ TODO | ✅ Реализовано |
| Unit-тесты | ❌ Заглушки | ✅ Полное покрытие |

---

## 🎓 Для преподавателей

### Оценка работ студентов

1. **Откройте:** [`ЧЕК-ЛИСТ_ДЛЯ_ПРОВЕРКИ.md`](./ЧЕК-ЛИСТ_ДЛЯ_ПРОВЕРКИ.md)
2. Следуйте пошаговым инструкциям
3. Используйте готовые curl команды для проверки
4. Сравните код студента с этим эталоном

### Критерии оценки

| Задание | Баллы |
|---------|-------|
| Удаление лайка | 5 |
| Редактирование комментария | 5 |
| Удаление комментария | 5 |
| Каскадное удаление поста | 5 |
| Unit-тесты (бонус) | +10 |
| **ИТОГО** | **20-30** |

### Типичные ошибки студентов

1. **Нет `GREATEST(..., 0)`** — likes_count становится отрицательным
2. **Нет `@Transactional`** — inconsistent state при ошибках
3. **SQL инъекция** — не используют параметризованные запросы
4. **Не обновляют счётчики** — comments_count не синхронизирован
5. **Неправильный порядок удаления** — пост до комментариев (foreign key error)

---

## 🔧 Дополнительная информация

### Конфигурация

- **Backend порт:** 8080
- **Frontend порт:** 3000
- **БД:** H2 in-memory (jdbc:h2:mem:testdb)
- **H2 консоль:** http://localhost:8080/h2-console

### Логирование

Логи сохраняются в:
- Консоль Tomcat
- `logs/application.log` (если настроено)

Уровень логирования настраивается в `logback.xml`.

### CORS

Настроен в `CorsFilter.java` для разрешения запросов с `http://localhost:3000`.

---

## 📞 Контакты

Если возникли вопросы по проверке работ:
- Обратитесь к старшему преподавателю
- Изучите документацию в проекте
- Сравните с этим эталонным решением

---

## ✅ Статус проекта

- ✅ Backend: Полностью реализован
- ✅ Frontend: Полностью реализован
- ✅ Тесты: Покрытие >80%
- ✅ Документация: Полная
- ✅ Чек-лист: Готов

**Проект готов для использования в учебном процессе!**

---

**Удачи в преподавании! 🎓**
